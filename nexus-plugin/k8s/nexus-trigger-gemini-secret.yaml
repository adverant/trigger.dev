# Template — replace <BASE64_API_KEY> before applying.
# Generate with: echo -n 'YOUR_API_KEY' | base64
apiVersion: v1
kind: Secret
metadata:
  name: nexus-trigger-gemini
  namespace: nexus
  labels:
    app: nexus-trigger
    app.kubernetes.io/component: health-monitor
type: Opaque
data:
  GEMINI_API_KEY: <BASE64_API_KEY>
